version: '3'
services:

  fleet-api:
    build:
      context: ./fleet-api
#      target: development
    volumes:
      - ./fleet-api:/usr/src/app
      - /usr/src/app/node_modules
    command:
      npm run start:dev
